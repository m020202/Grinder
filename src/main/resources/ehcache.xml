<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.ehcache.org/v3">
    <cache alias="default">
        <expiry>
            <ttl unit="seconds">60</ttl> <!-- TTL 설정: 1시간 -->
        </expiry>
        <heap unit="entries">10000</heap> <!-- 힙 크기: 1000 엔트리 -->
    </cache>
</config>